export function return_sum(numbers){
    let sum = 0;
    for (let i = 0; i < numbers.length; i++){
        if (typeof numbers[i] !== 'number'){
            var err = new Error('Input must be an array of numbers');
            return err.stack;
        }
        sum += numbers[i];
    }
    return sum;
}