export function return_absolute_value(number){
    if (typeof number !== 'number'){
        var err = new Error('Input must be number');
        return err.stack;
    }
    if (number > 0)
        return number;
    if (number < 0)
        return -number;
    return 0;
}